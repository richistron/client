import React from 'react';
// import PropTypes from 'prop-types';

class Tenant extends React.PureComponent {
  render() {
    return (
      <div>
        Tenant Select
      </div>
    );
  }
}

export default Tenant;
